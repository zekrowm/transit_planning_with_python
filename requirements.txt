# =============================================================================
# DEFAULT STACK — non-ArcGIS environments  (a.k.a. the GeoPandas stack)
# =============================================================================
# This is the canonical install target:
#
#     pip install -r requirements.txt
#
# Use this on personal machines, Linux/macOS systems, and CI — anywhere you
# are NOT running inside the ArcGIS Pro Python environment.
#
# If you ARE running inside ArcGIS Pro, see requirements-arcpro.txt — it
# documents the versions ArcPro 3.1 ships with for reference. Do not try to
# pip-install this file into the ArcPro Python: geopandas conflicts with
# arcpy at the binary level (GDAL versions), and your IT policy may block it.
#
# Dev/test/typing tooling lives in pyproject.toml under [dependency-groups];
# install with `pip install --group dev --group typing` (pip 25.1+ or uv).
# -----------------------------------------------------------------------------

# Python 3.12 target

# Core data science
numpy==1.26.4
pandas==2.2.3
scipy==1.12.0

# Network analysis
networkx==2.8.4

# Visualization
matplotlib==3.6.0
seaborn==0.12.1

# Excel I/O
openpyxl==3.1.5
xlrd==2.0.1

# Geospatial (open-source replacement for arcpy)
geopandas==1.1.1
shapely==2.1.1
pyproj==3.7.1

# String matching
rapidfuzz==3.13.0

# Optimization
pulp==2.9.0
